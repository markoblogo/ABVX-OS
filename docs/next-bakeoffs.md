# Next bakeoffs

Evaluate the development system before expensive platform work. FOUNDATION-002 establishes the minimal harness for experiment A; it does not select a provider. The remaining order is a proposal, not a selection:

1. **FOUNDATION-004 — development harness efficiency:** completed as an inconclusive static qualification; a reproducible model-agent adapter is still required before external skills can be selected.
2. **FOUNDATION-005 / PROJECT-ONBOARDING-001 — first real project onboarding:** completed for CoqPi as a conditional pass. The LoopX pilot used only the admitted mission-state boundary; the real call remains unvalidated and human-gated.
3. **FOUNDATION-006 — CoqPi live-call readiness:** proposed only. With explicit human consent, validate one bounded real microphone/OpenAI Realtime session, failure recovery, privacy receipt, and post-call evidence. Do not start automatically.
4. **Future — project/code intelligence:** compare narrow repository indexing and context extraction against a no-index baseline.
5. **Future — explicit cross-project context:** compare least-privilege context packages, provenance, and isolation against manual handoff.

Each experiment uses the bakeoff protocol and ends at `STOP_FOR_HUMAN_DECISION`. Do not assume a candidate wins from its feature list.
