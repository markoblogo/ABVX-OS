# Moderate feature fixture

Add a stable display string for labels. Reuse the existing `normalize_label` helper, preserve input order, and avoid dependencies or a second normalization implementation.
